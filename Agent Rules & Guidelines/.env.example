# Copy this file to .env and fill in your own keys after downloading the app.
# Do not commit .env – it is ignored by git. No developer API keys are shipped.

# Optional: Gemini API key for relevance scoring and email drafting (get one at https://aistudio.google.com/apikey)
VITE_GEMINI_API_KEY=
